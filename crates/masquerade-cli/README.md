# masquerade-cli

The standalone command-line interface for the `masquerade` media validation
library.

```console
$ masquerade upload.jpg
upload.jpg: valid
```

Use `--signature-only` to run only the bounded signature check.
